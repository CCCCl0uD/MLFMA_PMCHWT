请根据已有代码提取L算子和K算子代码函数

背景：
- 该项目是使用矩量法及其加速算法计算目标RCS的
- 其中MoM算法是重点，涉及的代码文件为MoM.h,MoM.cpp,EFiE.h,EFIE.cpp,MFIE.h,MFIE.cpp,Zmartix.h,Vm.h,RCS.h

范围：
- MoM算法计算pec物体的相关代码
- 不修改原版接口
- 重新写一个头文件存放提取的L算子和K算子代码

最终要求：

- 根据下方公式和原有代码，只将新写的矩量法的阻抗矩阵填充放入新的代码文件中，将原有结构中的K算子和L算子提取到相关函数中，其他结构都可以复用原有代码
- 注意不要遗忘伽辽金检验



下面给出MoM计算pec目标时的公式

EFIE方程
$$
\hat{\mathbf{n}} \times \mathcal{L}(\mathbf{\bar{J}}_{s}) = \hat{\mathbf{n}} \times \mathbf{E}^i(\mathbf{r}), \quad \mathbf{r} \in S
$$
MFIE方程
$$
\frac{1}{2}\mathbf{\bar{J}}_{s} + \hat{\mathbf{n}} \times \mathcal{K}(\mathbf{\bar{J}}_{s}) = \hat{\mathbf{n}} \times \mathbf{H}^i(\mathbf{r}), \quad \mathbf{r} \in S
$$
L算子
$$
\mathcal{L}(\mathbf{X}) = \mathrm{j}k_0 \oiint_{S_0} \left[ \mathbf{X}(\mathbf{r}') G_0(\mathbf{r}, \mathbf{r}') + \frac{1}{k_0^2} \nabla' \cdot \mathbf{X}(\mathbf{r}') \nabla G_0(\mathbf{r}, \mathbf{r}') \right] \mathrm{d}S'
$$
K算子
$$
\mathcal{K}(\mathbf{X}) =\oiint_{S_0} \mathbf{X}(\mathbf{r}') \times \nabla G_0(\mathbf{r}, \mathbf{r}') \, \mathrm{d}S'
$$
格林函数
$$
G_0(\mathbf{r}, \mathbf{r}') = \frac{e^{-jk_0|\mathbf{r} - \mathbf{r}'|}}{4\pi|\mathbf{r} - \mathbf{r}'|}
$$
CFIE公式
$$
\mathrm{CFIE}=\alpha \mathrm{EFIE}+\eta(1-\alpha)\mathrm{MFIE}
$$
$\alpha$ 为比例系数 ，PhyConstant.h 中的常系数alpha 就是我使用的 $\alpha$

#### 注意

求解矩量法需要使用伽辽金检验，我使用的基函数和测试函数都为RWG基函数，这一点在我的代码中有体现。而在上面公式中是没有体现这一步的，请注意。

